{
  "name": "js-handloom",
  "private": true,
  "description": "J.S. Handloom static site - build script stitches partials + product catalog into dist/",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node scripts/build.js",
    "preview": "node scripts/build.js && npx --yes serve dist"
  }
}
